server:
  port: 8084
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/joaZabala/microoservicios
          search-paths: config-data
  application:
    name: config-service

